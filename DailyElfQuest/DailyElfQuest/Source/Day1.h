#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <cstring>

using namespace std;

class Day1
{
public:
	vector<string> ExtractWordsFromFile(const string& filename);
	int SizeOfChar(const char* str);
	void SolutionDayOne();

};
